\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{cvpr}[2024/01/01 minimal local stand-in for the CVPR style]
% NOTE: temporary verification stub only. For the real CVPR-formatted build,
% replace this file with the official cvpr.sty from the CVPR LaTeX template.
\newif\ifcvprfinal
\newcommand{\cvprfinalcopy}{\cvprfinaltrue}
\endinput
